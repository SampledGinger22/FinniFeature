// Placeholder barrel for @finni/shared — the single source of truth for types, enums,
// Zod schemas, and DateTimeUtil. Real contract lands in Step 1 (spec §15).
export const SHARED_PACKAGE_NAME = '@finni/shared';
